---
description: UI/UX design specifications and component standards
alwaysApply: true
---

# UI/UX Specifications

## Design System

- **Component library**: shadcn/ui as the base — copy-paste, fully customizable
- **Styling**: Tailwind CSS with design tokens via CSS variables in `globals.css`
- **Icons**: Lucide React (consistent, tree-shakable)
- **Font**: Google Sans (all text — body, headings, code, data) via Google Fonts stylesheet — single typeface for unified visual identity (weights: 400, 500, 600, 700)

## Typography Scale (Custom — Scaled Up from Tailwind Defaults)

Font sizes are overridden in `globals.css` `@theme` block. Base body text (`text-sm`) = 16px.

| Utility | Size | Line Height | Usage |
|---------|------|-------------|-------|
| `text-xs` | 14px (0.875rem) | 20px | Captions, badges, helper text |
| `text-sm` | 16px (1rem) | 24px | Body text, labels, buttons (base) |
| `text-base` | 18px (1.125rem) | 28px | Emphasized body, subtitles |
| `text-lg` | 20px (1.25rem) | 30px | Section intros, card titles |
| `text-xl` | 24px (1.5rem) | 32px | Section headings |
| `text-2xl` | 28px (1.75rem) | 36px | Page sub-headings |
| `text-3xl` | 32px (2rem) | 40px | Page headings |
| `text-4xl` | 40px (2.5rem) | 48px | Hero headings (mobile) |
| `text-5xl` | 52px (3.25rem) | 1 | Hero headings (tablet) |
| `text-6xl` | 64px (4rem) | 1 | Hero headings (desktop) |

## Primary Color Palette

| Swatch | Hex | Usage |
|--------|-----|-------|
| Dark Blue | `#6696C9` | Primary accent, borders, interactive elements, gradient start |
| Light Blue | `#B5DAF2` | Gradient end, light backgrounds, tinted surfaces |
| Black | `#000000` | Text, high-contrast elements, dark badges |
| Light Gray | `#F5F5F5` | Section backgrounds, muted surfaces |
| White | `#FFFFFF` | Cards, page background, content areas |

- Hero gradient: `linear-gradient(to bottom, #6696C9, #B5DAF2)`
- Blue-themed banners/alerts: `bg-[#B5DAF2]/30` background + `border-[#6696C9]` border
- Use these hex values directly when Tailwind's semantic tokens don't apply

## Core Principles

1. **Mobile-first responsive** — design for 320px up; breakpoints: `sm:640` `md:768` `lg:1024` `xl:1280`
2. **Accessible (WCAG 2.1 AA)** — semantic HTML, ARIA labels, keyboard navigation, 4.5:1 contrast
3. **Progressive disclosure** — show essentials first, reveal complexity on demand
4. **Optimistic UI** — reflect user actions immediately, reconcile with server state
5. **Consistent feedback** — every action produces visible feedback (toast, loading, state change)

## Required States for Every View

Each page/component must handle all five states:

- **Loading** — skeleton placeholders matching content layout (never spinners for page loads)
- **Empty** — helpful illustration + CTA guiding next action
- **Error** — clear message + retry action, never raw error text
- **Partial** — graceful degradation when some data is available
- **Success** — the default populated view

## Component Guidelines

```tsx
// Component file structure
// 1. Imports
// 2. Types (if component-specific)
// 3. Component (named export, never default)
// 4. Sub-components (if small and tightly coupled)

export function ResumeCard({ resume, onSelect }: ResumeCardProps) {
  // hooks first, then derived state, then handlers, then render
}
```

- Prefer composition over prop drilling — use compound components for complex UI
- Max 3 levels of conditional rendering — extract to sub-components beyond that
- All interactive elements need `focus-visible` styles and `aria-label` when text isn't visible
- Use `next/image` for all images with explicit `width`, `height`, and `alt`
- Forms use `react-hook-form` + Zod resolver for validation

## Shadows & Borders

- **No shadows by default** — the base `Card` component ships without `shadow-*` classes. Do NOT add shadows to panels, cards, or boxes unless explicitly requested.
- **Borders only** — use light gray borders (`border-gray-200`) for visual separation.
- **Hover states** — prefer `hover:bg-muted/40` or background-color transitions over `hover:shadow-*`.
- If a shadow is specifically requested, apply it only to that element — never re-introduce shadows globally.

## Layout

- Max content width: `max-w-7xl` (1280px) centered
- Consistent spacing scale: multiples of 4px (Tailwind default)
- Sidebar navigation for dashboard, top nav for public pages
- Sticky headers with blur backdrop on scroll
